POSTGRES_USER=postgres
POSTGRES_HOST=localhost
POSTGRES_DB=restaurant
POSTGRES_PASSWORD=ehrry
POSTGRES_PORT=5432

PORT = 5000
DATABASE_URL=postgresql://neondb_owner:npg_TcD4Qv7bzkaV@ep-fancy-queen-ahz4fsbd-pooler.c-3.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require
NODE_ENV=development

# const pool = new Pool({
#   user: process.env.POSTGRES_USER || "postgres",
#   host: process.env.POSTGRES_HOST || "localhost",
#   database: process.env.POSTGRES_DB || "restaurant",
#   password: process.env.POSTGRES_PASSWORD || "ehrry",
#   port: parseInt(process.env.POSTGRES_PORT || "5432", 10),
# });